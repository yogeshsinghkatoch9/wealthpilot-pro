<div class="empty-state empty-state-search empty-state-small fade-in" role="status">
  <div class="empty-state-icon">
    <svg fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
    </svg>
  </div>

  <div class="empty-state-content">
    <h3 class="empty-state-title">No Results Found</h3>
    <p class="empty-state-description">
      We couldn't find any matches for "<%= typeof searchQuery !== 'undefined' ? searchQuery : 'your search' %>". Try different keywords or filters.
    </p>

    <div class="empty-state-actions">
      <button onclick="clearSearch()" class="empty-state-action-secondary">
        Clear Search
      </button>
    </div>
  </div>
</div>
